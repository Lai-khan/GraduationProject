doctype html
html
    head
        include head
        link(rel='stylesheet', href='/stylesheets/board.css', type='text/css')
    body
        include nav
        section.hero.is-medium.is-primary.is-bold
            .hero-body
                .container.has-text-centered
                    h1.title
                        | 원하는 곡을 찾아보세요!
                    form.panel-block(action='/search/1')
                        .control.has-icons-left
                            input.input.is-rounded.is-medium.has-text-centered(type='text', placeholder='연주하고 싶은 노래를 검색해보세요.', name='musicName')
                            span.icon.is-left
                                i.fas.fa-search(aria-hidden='true')
        section.section.is-medium
            .container
                table.table.is-hoverable
                    thead
                        tr
                            th 번호
                            th 곡 제목
                            th 작성자
                            th 등록일
                    tbody
                    - var end = (page * page_num) < list.length ? (page * page_num) : list.length
                    - for (var i = page*page_num - page_num; i < end; i++)
                        if i > list.length-1
                            - i++;
                        else
                            - var data = list[i];
                        tr
                            td #{data.idx}
                            td
                                - var link = data.idx;
                                a(href='/guitar/'+link) #{data.title}
                            td #{data.name}
                            td #{data.updateDate}
                nav.pagination.is-rounded.is-centered(role='navigation', aria-label='pagination')
                    a.pagination-previous Previous
                    a.pagination-next Next page
                    ul.pagination-list
                        li
                            a.pagination-link(aria-label='Goto page 1') 1
                        li
                            span.pagination-ellipsis …
                        li
                            a.pagination-link(aria-label='Goto page 5') 5
                        li
                            a.pagination-link.is-current(aria-label='Page 6', aria-current='page') 6
                        li
                            a.pagination-link(aria-label='Goto page 7') 7
                        li
                            span.pagination-ellipsis …
                        li
                            a.pagination-link(aria-label='Goto page 10') 10
        include footer